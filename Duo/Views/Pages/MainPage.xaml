<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DuolingoNou.Views.Pages.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DuolingoNou.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <GridView x:Name="SuggestedFriendsGridView" 
                  HorizontalAlignment="Right" 
                  VerticalAlignment="Bottom" 
                  Width="300" 
                  Height="200" 
                  Background="White"
                  BorderBrush="Black"
                  BorderThickness="2"
                  Margin="10,10,35,35">
            <GridView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Padding="5">
                        <!-- Profile Image -->
                        <Ellipse Width="30" Height="30" VerticalAlignment="Center">
                            <Ellipse.Fill>
                                <ImageBrush ImageSource="{Binding ProfileImage}" Stretch="UniformToFill"/>
                            </Ellipse.Fill>
                        </Ellipse>
                        <!-- User Name -->
                        <TextBlock Text="{Binding UserName}" VerticalAlignment="Center" Width="270"/>
                    </StackPanel>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</Page>
