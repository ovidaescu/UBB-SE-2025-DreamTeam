<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="DuolingoNou.Views.Pages.SignUpPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DuolingoNou.Views.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid VerticalAlignment="Center" HorizontalAlignment="Center" Padding="20">
        <StackPanel Spacing="15" Width="300">
            <TextBlock Text="Sign Up" FontSize="24" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <TextBox PlaceholderText="Username" Text="{Binding NewUser.UserName, Mode=TwoWay}"/>
            <TextBox PlaceholderText="Email" Text="{Binding NewUser.Email, Mode=TwoWay}"/>
            <PasswordBox x:Name="PasswordBoxWithRevealMode" />

            <CheckBox x:Name="RevealModeCheckBox" Content="Show Password" Checked="RevealModeCheckbox_Changed" Unchecked="RevealModeCheckbox_Changed" />


            <Button Content="Create Account" Click="OnCreateUserClick"/>
            <Button Content="Already have an account? Login"
                Click="NavigateToLoginPage"
                HorizontalAlignment="Center"
                Foreground="Blue"
                BorderThickness="0"
                FontSize="14"
                Padding="5"/>

        </StackPanel>
    </Grid>
</Page>